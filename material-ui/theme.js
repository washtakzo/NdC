import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#E2D7D7",
      main: "#E2D7D7",
      dark: "#E2D7D7",
      contrastText: "#000",
    },
    secondary: {
      light: "#907070",
      main: "#907070",
      dark: "#907070",
      contrastText: "#fff",
    },
  },
});

export default theme;
